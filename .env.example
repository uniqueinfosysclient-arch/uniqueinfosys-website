# Supabase CMS (news strip, About gallery, download links).
# Copy to .env.local and fill in from Supabase → Settings → API.
# Leave unset to run the site without the CMS (falls back to built-in content).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
